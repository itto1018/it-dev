{
  "name": "it_dev",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-24.04",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "lts"
    }
  },
  "postCreateCommand": "npm install -g pnpm && pnpm install && pnpm exec playwright install --with-deps chromium"
}
